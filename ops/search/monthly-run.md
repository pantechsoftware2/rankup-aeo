# Monthly SEO and AEO run

Schedule status: `not_active` as of 2026-09-23. No supported scheduler, runner identity, or project credential was available in the repository.

When registered, run on the first day of each month at 09:00 Asia/Calcutta, with a 30-minute runtime limit, the saved crawl/AI budgets in `config.json`, and a project lock. Read `master-playbook.md`, `config.json`, `facts.json`, prior run, change register, content register, measurement contract, and prompt suite before making changes.

The runner must diagnose first, fix verified regressions, research only evidence-backed content opportunities, run technical/editorial/measurement QA, deploy through the existing Vercel workflow, verify the production domain, and retain rollback evidence. It must not publish when facts, access, or QA gates are missing. It must record no-change runs and release the lock.

Required setup: provide the supported scheduling surface, Vercel project/deployment access, Search Console and analytics access, and any approved AI test access. Do not claim that a cron endpoint alone is an active Codex research/editorial scheduler.
